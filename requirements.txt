# Core
torch>=2.4.0                    # MPS support is automatic on Apple Silicon
torchvision>=0.19.0
torchaudio>=2.4.0

# Hugging Face ecosystem
transformers>=4.44.0
datasets>=2.21.0
peft>=0.12.0                    # for LoRA
accelerate>=0.33.0
bitsandbytes>=0.43.3            # quantization (works on CPU/MPS too, though slower)
sentence-transformers>=3.0.1    # for RAG embeddings

# Data & efficiency
tiktoken>=0.7.0
numpy>=1.26.0
pandas>=2.2.0
tqdm>=4.66.0
pyarrow>=15.0.0

# RAG & vector search
faiss-cpu>=1.8.0                # faiss-gpu not needed on Mac

# Web / demo
fastapi>=0.112.0
uvicorn>=0.30.0
streamlit>=1.38.0

# Tracking & utils
mlflow>=2.15.0
pyyaml>=6.0.0
optuna>=3.6.0                   # hyperparam tuning (optional later)